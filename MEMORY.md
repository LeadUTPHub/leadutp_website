# MEMORY.md — Retrospectiva del workstream (panel de administración LEAD UTP)

> Se actualiza al cerrar cada tarea y cada sprint (regla de `AGENTS.md`).
> Distinto de `CONTEXT.md` (estado del sitio público) y de `.sprints/BACKLOG.md` (plan).

## Estado del sistema

| Dimensión | Estado a 2026-09-10 |
|---|---|
| Rama | `cms-admin` (oficial, sin prefijo `feature/`). Nunca se commitea a `main`. |
| Paso de la metodología | Paso 5 (desarrollo sprint por sprint) en curso; **Sprints 0, 1, 2, 3, 4 y 5 completos técnicamente (6/6, 6/6+1, 8/8, 6/6, 7/7, 6/6).** Sprint 5 reportado, esperando aprobación explícita del PO antes de abrir Sprint 6. |
| Sprint en curso | **Ninguno abierto.** Sprint 5 (6/6): validador de `page_blocks.data`, repo+endpoints, UI `/administrator/paginas`, loaders DB→snapshot→`.data.ts`→vacío de `/nosotros` y `/proyectos`, script `prebuild` que genera/borra `*.fallback.json`, y verificación de punta a punta contra Supabase real (incluida una caída simulada de verdad, no solo unit tests). D-14(f) **resuelto por el VP: lista centralizada** de proyectos. Sprint 6 (pulido UX/UI) no arranca sin confirmación explícita del PO. |
| Sitio público | `/eventos` muestra Luma (botón + iframe opcional, ahora diferido con `IntersectionObserver`, L29) + tarjetas de punteros `published && featured` (Sprint 3) + galerías `published` más recientes con sus fotos y texto en markdown ("Eventos realizados", Sprint 4, fotos por galería en 1 sola query — L29) en vez de los 22 eventos ficticios; Home muestra un CTA simple a Luma mientras no haya `featuredEvent` local (`HomeEventsCta`, T1.7). `/nosotros` (historia + junta) y `/proyectos` (Sprint 5, T5.4) leen de `page_blocks` en build-time con la cadena DB→snapshot→`.data.ts`→vacío — snapshot todavía no existe (T5.5 pendiente), así que hoy es DB→`.data.ts`→vacío en la práctica. `astro.config.mjs` con adaptador de Vercel; build genera 15 páginas HTML + **1 función** (`/administrator/**`). |
| `/administrator` | **Funcional de punta a punta**, incluyendo contenido. Login, dashboard, logout; **`/administrator/eventos`** (Sprint 3, CRUD de punteros), **`/administrator/galerias`** (Sprint 4, CRUD de galerías + subida de fotos a Cloudinary con progreso real) y **`/administrator/paginas`** (Sprint 5, T5.3 — historia/junta/proyectos, solo editable por `super_admin`, D-13; resto del staff en modo lectura) — todos con autorización 100% delegada a la RLS, acciones condicionadas a `canEdit()`. Sidebar con los 3 links activos. Middleware protege todo excepto `/administrator/login`. Oculta (sin enlaces, fuera de sitemap, `robots.txt` con `Disallow`). MFA-ready, TOTP no exigido (D-P3). |
| Supabase | Proyecto creado por el PO, 6 env vars en `.env` (nomenclatura nueva). `database/schema.sql` aplicado y verificado (T0.3); `luma_event_pointers` migrado a tarjeta completa en Sprint 3. `event_galleries`/`gallery_photos` no necesitaron migración (el schema de Sprint 0 ya alcanzaba). |
| Cloudinary | Cuenta creada y **verificada end-to-end** desde T0.4, y ahora **en uso real** (Sprint 4): firma de subida (sin `max_file_size`, ver L25/D-12), subida directa desde el navegador con progreso, borrado best-effort de assets al borrar galería/foto. |
| Docs generados | `AGENTS.md`, `ARCHITECTURE.md`, `TECH_STACK.md`, `DOMAIN.md`, `DESIGN.md`, `database/schema.sql`, `.sprints/BACKLOG.md`, `docs/API_CONTRACTS.md`, `docs/REQUISITOS_ADMIN.md`, `docs/DISENO_TECNICO.md`. |
| Tests | 384 tests, 36 archivos — incluye `rls.integration.test.ts` (7), `SupabaseContentRepository.pointers.test.ts` (8), `SupabaseContentRepository.galleries.test.ts` (21) y `SupabaseContentRepository.pageBlocks.test.ts` (13), todos contra Supabase real, se saltan sin credenciales de test. Todos en verde (384/384). |
| Código Sprint 0 | `src/env.d.ts`, `src/domain/{types.ts, ports/*, purity.test.ts}`, `src/infra/{container.ts, noop/*, build-boundary.test.ts}`, `scripts/verify-cloudinary-upload.mjs`. |
| Código Sprint 1 | `src/domain/lumaLinks.ts` (+ test) · `src/components/events/EventsLuma.astro` · `src/components/home/HomeEventsCta.astro` (extra) · `Button.astro` extendido (`target`/`rel` opcionales) · `events.data.ts` vaciado · `eventos.astro` y `index.astro` reescritos en las partes de eventos. |
| Código Sprint 2 | `src/middleware.ts` (+ test) · `src/infra/supabase/{cookieHeader,mapUserToProfile,createCookieAdapter,SupabaseAuthGateway}.ts` (+ tests) · `src/infra/supabase/rls.integration.test.ts` · `src/pages/administrator/{login,index,logout}.astro` · `src/components/admin/{Field,AdminShell}.astro` · `src/layouts/AdminLayout.astro` · `src/infra/admin-route-hidden.test.ts` · `database/patches/001-fix-hook-security-definer.sql`. |
| Código Sprint 3 | `src/domain/{canEdit,validateEventPointerInput,validateEventPointerPatch,selectFeaturedPointers}.ts` (+ tests) · `src/domain/ports/ContentRepository.ts` extendido (`EventPointer`) · `src/infra/supabase/SupabaseContentRepository.ts` (+ test de contrato contra Supabase real) · `src/pages/administrator/api/event-pointers/{index,[id],_shared}.ts` (+ tests) · `src/pages/administrator/eventos.astro` · `src/components/events/EventPointerCard.astro` · `Field.astro` extendido (textarea/select) · `database/patches/002-event-pointers-full-card.sql`. |
| Código Sprint 4 | `src/infra/cloudinary/{buildSignature,CloudinaryPhotoStorage}.ts` (+ tests) · `src/domain/buildCloudinaryUrl.ts` (+ test) · `src/domain/ports/PhotoStorage.ts` extendido (`destroy`) · `src/domain/{validateGalleryInput,validateGalleryPatch,validateGalleryPhotoInput,validateGalleryPhotoPatch}.ts` (+ tests) · `src/domain/ports/ContentRepository.ts` extendido (`Gallery`, `GalleryPhoto`) · `src/infra/supabase/SupabaseContentRepository.ts` extendido (+ `SupabaseContentRepository.galleries.test.ts`, 21 tests contra Supabase real) · `src/pages/administrator/api/uploads/sign.ts` · `src/pages/administrator/api/galleries/**` (+ tests) · `src/pages/administrator/galerias.astro` y `galerias/[id].astro` · `src/components/events/GalleryEventCard.astro` · `src/infra/markdown/renderMarkdown.ts` (+ test, `marked`+`sanitize-html`) · `Button.astro` (fix de reenvío de `data-*`, L24) · `AdminShell.astro` (link de Galerías). |
| Código Sprint 5 (6/6) | `database/patches/003-page-blocks-drop-area-not-null.sql` (D-13) · `src/domain/validatePageBlockData.ts` (+ 34 tests, T5.1) · `src/domain/ports/ContentRepository.ts` extendido (`PageBlock`/`PageBlockInput`) · `src/infra/supabase/SupabaseContentRepository.ts` extendido (`getPageBlock`/`savePageBlock`, upsert en 2 pasos, no `.upsert()`, para no pisar `owner_id` — ver L27) + `SupabaseContentRepository.pageBlocks.test.ts` (13 tests contra Supabase real) · `src/pages/administrator/api/pages/[key].ts` (+ test, T5.2) · `src/domain/pageBlockForms.ts` (+ 18 tests) · `src/pages/administrator/paginas.astro` · `AdminShell.astro` (link de Páginas, T5.3) · `src/infra/content/{resolvePageContent,loadPageContent}.ts` (+ test, T5.4) · `src/pages/nosotros.astro`/`proyectos.astro` (loaders + `renderMarkdown()` en historia) · `src/infra/content/buildPageSnapshots.ts` (+ 14 tests, política pura de qué persistir por key) · `scripts/generate-page-snapshots.ts` (I/O, corre con `node` nativo — Node 22.6+/24 hace *type stripping* de `.ts`, sin `tsx`/`ts-node`, ver L30) · `package.json` (`prebuild`, hook de pnpm/npm antes de `build`). Además, fuera de alcance de sprint: `src/components/events/EventsLuma.astro` (iframe diferido con `IntersectionObserver`) y `SupabaseContentRepository.listPhotosForGalleries` (+ test, `eventos.astro`) — ver L29. |
| Vercel | Validación de cada sprint sigue siendo 100% local en `cms-admin` (`pnpm dev`; ver L20 — `pnpm preview` no es compatible con `@astrojs/vercel`). No hay preview de PR (ver D-P7). **Aparte de eso**, `main` sí se despliega de verdad en `leadutp.vercel.app` — el PO mergeó `cms-admin`→`main` (PR #8) y `feature/pruebas`→`main` (PR #9) directamente en GitHub, antes de lo previsto por el Paso 6, para que la comunidad vaya viendo la página. No existe un ambiente de staging separado: "no tengo vercel, todo es real" (PO). Ver D-08. |

## Lecciones aprendidas

| # | Fecha | Lección | Origen |
|---|---|---|---|
| L1 | 2026-09-10 | En Astro 7 **no existe `output: 'hybrid'`**. El equivalente es `output: 'static'` + adaptador + `export const prerender = false` por ruta. La guía usa "hybrid" en sentido conceptual. | Paso 1–2 |
| L2 | 2026-09-10 | Tailwind 4 en este repo **no tiene `tailwind.config.js`**: los tokens viven en `@theme` dentro de `src/styles/global.css`. Cualquier búsqueda de "config de Tailwind" va ahí + a `pillarGradients` en `HomePillars.astro`. | Paso 1 |
| L3 | 2026-09-10 | El iframe de Google Forms en `/convocatorias` ya demostró que un embed cross-origin **siempre** se ve con fondo blanco y no hereda el tema. Se asume lo mismo para el embed de Luma → estrategia "enlace-first + iframe opcional". | Paso 2 |
| L4 | 2026-09-10 | `docs/GUIA_METODOLOGIA_ADMIN.md` contenía 12 menciones a `feature/cms-admin` en su cuerpo (flujo git, regla 10, Pasos 4–6) pese a que el PO indicó haberlo corregido. **RESUELTO (2026-09-10):** el reemplazo `feature/cms-admin` → `cms-admin` (+ la URL de preview `leadutp-git-feature-cms-admin-…` → `leadutp-git-cms-admin-…`) se aplicó directamente sobre el archivo del repo por el agente, no por reemplazo manual del PO. Verificado con grep: 0 ocurrencias de `feature/cms-admin` ni `feature-cms-admin`. | Paso 2 |
| L5 | 2026-09-10 | Los 22 eventos de `events.data.ts` son ficticios. En Sprint 1 se **vacía** el array (no se borra el archivo ni `events.utils.ts`, que lo usa `/pilares/[slug]`). | Paso 1 |
| L6 | 2026-09-10 | El repo **todavía no está conectado a Vercel** (más allá del deploy ya existente del sitio público desde `main`) — `git push origin cms-admin` hoy es solo respaldo en GitHub, no genera preview. Todo texto que asumía "validar en preview de Vercel" como gate de cada sprint (`AGENTS.md`, `.sprints/BACKLOG.md`) se corrigió a "validar en local" (`pnpm build && pnpm preview` + `pnpm dev`). Ver D-P7. | Corrección del PO tras el Paso 3 |
| L7 | 2026-09-10 | Este repo **no tiene `argsIgnorePattern: '^_'`** configurado en `@typescript-eslint/no-unused-vars` — prefijar un parámetro no usado con `_` (convención común en otros proyectos) sigue marcando error aquí. Patrón adoptado: referenciar el parámetro con `void nombre;` dentro del cuerpo de la función (ver `src/infra/noop/*.ts`). | Sprint 0, T0.5 (`pnpm lint`) |
| L8 | 2026-09-10 | Agregar `adapter: vercel()` crea `.vercel/output/` como nuevo artefacto de build (en paralelo a `dist/`). Hay que ignorarlo en **dos lugares independientes**: `.gitignore` (para no commitearlo) **y** `eslint.config.mjs` → `ignores` (si no, ESLint lintea el JS minificado de `.vercel/output/static/_astro/*.js` y produce decenas de falsos positivos `no-unused-expressions`). `dist/` ya estaba cubierto en ambos; `.vercel/` no lo estaba en ninguno hasta Sprint 0. | Sprint 0, T0.1/T0.5 |
| L9 | 2026-09-10 | Sin Supabase CLI instalado, el agente **no puede aplicar `database/schema.sql`** de forma autónoma — requiere pegarlo en el SQL Editor del dashboard de Supabase (acción del PO) o instalar y enlazar el CLI. Se trató como acción externa que requiere confirmación explícita, igual que la subida de prueba a Cloudinary (T0.4). | Sprint 0, T0.3 |
| L10 | 2026-09-10 | La viabilidad de un iframe cross-origin **se puede verificar sin acceso al dashboard**: `curl -I` a la URL y revisar `X-Frame-Options`/`Content-Security-Policy: frame-ancestors` (y opcionalmente buscar JS anti-framing tipo `self !== top` en el HTML). Así se confirmó que `luma.com/leadutp_` bloquea el framing pero `luma.com/embed/calendar/<slug>/events` no — y que ese path de embed funciona **directo con el slug público**, sin necesitar el `cal-id` privado que se asumía en el Paso 2. | Sprint 1, T1.2 |
| L11 | 2026-09-10 | `Button.astro` no tenía forma de abrir en pestaña nueva. Se extendió con `target`/`rel` **opcionales, sin default más que `undefined`** — Astro omite un atributo `undefined` al renderizar, así que las 10 invocaciones preexistentes (7 archivos) quedan byte-idénticas. Patrón a seguir: extender un componente `ui/` con props opcionales antes que crear una variante nueva o un `<a>` suelto. | Sprint 1, T1.2 |
| L12 | 2026-09-10 | Vaciar `events.data.ts` tiene un efecto en cascada ya contemplado por el propio código: `index.astro` (Home) también lee `events` para su sección `HomeEvents`, y ya estaba guardada con `{featuredEvent && ...}` — se omite sola, sin romper nada. Al detectarlo se le avisó al PO antes de decidir qué hacer (ver D-05). | Sprint 1, T1.4 |
| L13 | 2026-09-11 | `astro:middleware` (el helper `defineMiddleware`) es un módulo virtual que solo resuelve el plugin de Vite de Astro — Vitest en aislamiento no puede importarlo. `onRequest` se exporta como función `async` normal, tipada con `APIContext`/`MiddlewareNext` de `astro` (import de tipos, sí resuelve), sin el wrapper. Es puramente un helper de tipos; el runtime de Astro no lo exige. | Sprint 2, T2.1/T2.3 |
| L14 | 2026-09-11 | Crear `src/middleware.ts` (o cualquier archivo de convención especial de Astro) **con el dev server ya corriendo no lo registra en caliente** — dio 404 en `/administrator` hasta reiniciar (`astro dev stop` + `astro dev --background`). Después de eso, funcionó. Revisar esto primero ante un 404 inesperado en una ruta que debería estar protegida. | Sprint 2, T2.3 |
| L15 | 2026-09-11 | En Windows sin **Developer Mode** activado, `pnpm build` con el adaptador de Vercel falla con `EPERM: symlink` al empaquetar la primera función serverless real (necesita symlinkear `sharp` como dependencia nativa; pnpm evita esto con junctions, pero `@vercel/nft` usa symlinks de archivo, que exigen el privilegio `SeCreateSymbolicLinkPrivilege`). No es un bug del código — en Vercel (Linux) no pasa. Fix: activar Developer Mode (Configuración → Privacidad y seguridad → Para desarrolladores) o correr el build como Administrador. Confirmado que persiste incluso sin usar `<Image>` de `astro:assets` en la página nueva — Astro registra el endpoint `/_image` en cuanto hay adaptador, sin importar si esa página SSR específica usa `<Image>`. **RESUELTO (2026-09-11):** el PO activó Developer Mode; `pnpm build` corre limpio en terminal normal, sin admin. | Sprint 2, T2.4 (primer build con una ruta server-rendered real) |
| L16 | 2026-09-11 | `@astrojs/vercel` sin `functionPerRoute` empaqueta **todas** las rutas `prerender=false` en una sola función (`_render.func`), no una por ruta. `EXPECTED_FUNCTION_COUNT` en `build-boundary.test.ts` es 1 aunque `/administrator`, `/administrator/login` y `/administrator/logout` sean 3 páginas distintas — hay que tenerlo en cuenta si en algún sprint se activa `functionPerRoute` (el número esperado subiría). | Sprint 2, T2.8 |
| L17 | 2026-09-11 | **Bug:** `loading="lazy"` en un `<iframe>` dentro de un contenedor `hidden` (`EventsLuma.astro`) nunca dispara `load` ni `error` — el navegador no puede calcular "cerca del viewport" para un elemento sin layout (`display:none` vía `hidden`), así que la carga lazy nunca arranca. Síntoma exacto: cero peticiones de red, cero errores de consola, atascado en "Cargando…" para siempre — parecía un bug del timeout/listener, pero el candado era la combinación `hidden` + `loading="lazy"` sobre el mismo elemento. Regla general: si el JS propio decide cuándo revelar un elemento tras su `load`, **no** le sumes `loading="lazy"` encima — son dos mecanismos de "cuándo cargar/mostrar" que se pisan. | Sprint 2 (post-Sprint 1), bug en `EventsLuma.astro` |
| L18 | 2026-09-11 | **Bug:** `custom_access_token_hook` (Sprint 0) se escribió sin `SECURITY DEFINER`. Mientras el hook no estaba registrado en Authentication → Hooks no se notaba (la función simplemente no corría). Al activarlo, el login empezó a fallar con "Error running hook... permission denied for table profiles": el hook lo ejecuta el rol `supabase_auth_admin`, que no tiene grant sobre `profiles`, y en pleno login todavía no hay `auth.uid()` válido para que la RLS deje pasar la lectura igual. **Regla general para Auth Hooks de Supabase que leen tablas con RLS:** casi siempre necesitan `SECURITY DEFINER` — si no, dependen de que el rol interno de Auth tenga acceso, cosa que no tiene por defecto ni debería tener. Fix en `database/patches/001-fix-hook-security-definer.sql`, ya incorporado a `database/schema.sql` para proyectos nuevos. **Nota de diagnóstico:** `data.user.app_metadata` del response de `signInWithPassword` es el `raw_app_meta_data` crudo de `auth.users`, no las claims que el hook inyecta — para ver esas hay que decodificar el JWT (`data.session.access_token`, payload en base64url) manualmente. | Sprint 2, T2.6 |
| L19 | 2026-09-11 | **Bug (mío, T2.2):** `SupabaseAuthGateway.buildSession()` armaba el `Profile` a partir de `data.user` (de `signInWithPassword()`/`getUser()`) — el mismo error de L18: ese `user.app_metadata` es el crudo de `auth.users`, sin las claims del hook, así que `mapUserToProfile()` siempre devolvía `null` → `signInWithPassword()` tiraba "Este usuario no tiene un perfil asignado" → `login.astro` lo mostraba como el genérico "Usuario o contraseña incorrectos" (regla de no filtrar detalle, D2), ocultando que la causa real no tenía nada que ver con la contraseña. El test de integración de RLS (T2.6) no lo detectó porque usa `createClient` + queries directas, nunca pasa por `SupabaseAuthGateway`. **Fix:** usar `supabase.auth.getClaims()` en vez de `getUser()`/`data.user` — es la API pensada para esto (la propia doc de auth-js dice "the returned claims can be customized... using the Custom Access Token Hook"), y de paso ya trae el `aal` directo, así que se pudo borrar `readAssuranceLevel()`/`getAuthenticatorAssuranceLevel()` por completo. **Regla general:** con un Auth Hook activo, siempre leer claims con `getClaims()`, nunca con `user.app_metadata` de `getUser()`/`signInWithPassword()`/`signUp()`. | Sprint 2, T2.8 (diagnóstico pedido por el PO: login manual fallaba con las mismas credenciales que sí funcionaban en T2.6) |
| L20 | 2026-09-11 | `pnpm preview` **no funciona con `@astrojs/vercel`, por diseño** — el propio adaptador tira un error explícito diciendo que no soporta el comando `preview` (confirmado por el PO: no es específico de Windows ni de esta versión de Node). No es deuda técnica ni un bug a investigar. Mientras Vercel no esté conectado, la validación local de cada sprint es con `pnpm dev` (que sí sirve las mismas rutas/middleware/RLS). Alternativa futura si hiciera falta: `vercel dev` (requiere vincular el proyecto con la CLI de Vercel). | Sprint 3, T3.6 |
| L21 | 2026-09-11 | **Bug:** poner un archivo `*.test.ts` dentro de `src/pages/` (para colocar el test junto al endpoint que prueba) rompe `pnpm build` — Astro trata **cualquier** archivo sin prefijo `_` bajo `src/pages/` como una ruta real, sin importar la extensión `.test.ts`. Intentó prerenderizar los tests, encontró `vi.mock()` (inexistente fuera de Vitest) y el proceso crasheó con un assert de libuv en Windows (mensaje confuso, nada que ver con la causa real). **Fix:** prefijo `_` en el nombre del archivo (`_index.test.ts`, no `index.test.ts`) — Astro lo excluye del ruteo, Vitest lo sigue encontrando igual (solo le importa el sufijo `.test.ts`). Regla general: todo archivo bajo `src/pages/` que no sea una ruta real (tests, helpers compartidos como `_shared.ts`) necesita el prefijo `_`. | Sprint 3, T3.3 |
| L22 | 2026-09-11 | Correr en paralelo (`pnpm vitest run`, toda la suite) dos archivos de test que autentican **el mismo usuario de prueba** con `createClient(...).auth.signInWithPassword()` en clientes separados puede hacer que una llamada posterior a `client.auth.getUser()` (valida el token por red) falle con "No hay sesión válida", aunque ese mismo cliente funcionara segundos antes — no se detecta corriendo el archivo solo. `client.auth.getSession()` (lee el estado local del cliente, sin red) no tuvo el problema. Regla general para código que solo necesita el id del usuario ya autenticado (no revalidar el token): preferir `getSession()` sobre `getUser()`. | Sprint 3, T3.2 (`SupabaseContentRepository.createPointer`) |
| L23 | 2026-09-11 | La protección CSRF nativa de Astro (rutas `prerender=false`) exige que el header `Origin` de un `POST`/`PATCH`/`PUT`/`DELETE` coincida con el host — si falta o no coincide, responde `403` antes de que el código del endpoint corra. `curl` no manda `Origin` por defecto (un navegador sí, siempre). Al probar endpoints a mano con `curl` hay que agregar `-H "Origin: http://localhost:4321"` o el 403 se puede confundir con un bug de autorización propio (RLS, `canEdit`, etc.) cuando en realidad nunca llegó al handler. | Sprint 3, T3.4 (smoke test manual de `/administrator/api/event-pointers`) |
| L24 | 2026-09-11 | **Bug (afectaba Sprint 3 Y Sprint 4 a la vez, sin que nadie lo notara hasta probar clicks reales):** `Button.astro` destructuraba solo sus props declaradas (`href`, `variant`, `size`, ...) y nunca reenviaba el resto al `<button>`/`<a>` real del DOM — cualquier `data-*` usado como gancho de JS (`data-open-create`, `data-delete`, etc.) se perdía en silencio. El script de cada pantalla hacía `querySelector('[data-open-create]')`, no encontraba nada, y el `if (!openCreateBtn) return` cortaba **todo** `connectedCallback` sin avisar — ningún botón de esa pantalla respondía al clic. No se detectó en Sprint 3 porque la verificación fue solo por `curl` (API directa) + inspección de HTML, nunca clicando de verdad; recién se notó cuando el PO probó "Nueva galería" en un navegador real. **Fix:** `...rest` + `{...rest}` en `Button.astro`, reenvía cualquier prop no declarado. Regla general: un componente wrapper de un elemento nativo (`<button>`, `<a>`, `<input>`) debe reenviar props no declarados por defecto, salvo razón explícita para no hacerlo — si no, cualquier gancho de JS futuro se rompe en silencio y solo se detecta clicando de verdad, nunca con tests que mockean el DOM o pruebas de API. | Sprint 4, T4.5 (reportado por el PO probando la UI real) |
| L25 | 2026-09-11 | `max_file_size` **no es un parámetro válido para subidas firmadas de Cloudinary** — es una configuración de *upload preset* (subidas *unsigned*, descartadas por diseño en `DISENO_TECNICO.md` §2.1). Firmarlo invalida la firma para **toda** subida (confirmado con Cloudinary real: el 401 "Invalid Signature" expone el string que Cloudinary sí firmó, y `max_file_size` no aparece ahí); mandarlo sin firmar tampoco lo hace cumplir — Cloudinary lo ignora en silencio (confirmado: un archivo de 10MB pasó con `max_file_size=1MB`, 200 OK). Verificado contra la documentación oficial de Cloudinary (vía WebFetch): `max_file_size` aparece únicamente en el contexto de `upload_presets`, nunca como parámetro de un upload firmado ad-hoc. **Regla general:** con subidas firmadas (opción B de `DISENO_TECNICO.md`), solo se puede confiar en parámetros que Cloudinary demuestre incluir en su propio "string to sign" (`allowed_formats` sí lo hace) — cualquier otro límite (tamaño, cantidad) hay que hacerlo cumplir del lado propio (cliente y/o servidor), nunca asumir que Cloudinary lo va a validar solo porque se manda como parámetro. | Sprint 4, T4.5 (bug real reportado por el PO subiendo 2 fotos `.webp` reales) |
| L26 | 2026-09-11 | **Meta-lección de proceso, pedida explícitamente por el PO:** cuando aparecen varios bugs seguidos durante la validación de un sprint (Sprint 4: Button.astro, sidebar, duplicación pilar/área, markdown sin parsear, `max_file_size`), conviene **commitear cada fix en cuanto se cierra**, no acumularlos todos sin commitear hasta el final. Acumularlos entrelaza las dependencias de archivo entre fixes sucesivos (ver el commit `0542b44`, donde aislar "solo" el fix de `max_file_size` terminó arrastrando `SupabaseContentRepository.ts` completo y el fix de `Button.astro` porque nada de eso tenía un commit previo del que partir) y obliga a reconstruir agrupaciones después con más esfuerzo del necesario. | Sprint 4, retrospectiva pedida por el PO tras el cierre del bug de Cloudinary |

| L27 | 2026-09-11 | **Semántica de `NULL` en las políticas RLS de `area_slug` — y un caveat que hay que revisar si algún día se reasigna `owner_id`.** Al hacer `page_blocks.area_slug` nullable (D-13) no hizo falta reescribir ni una política: con `area_slug = NULL`, la comparación `area_slug = public.auth_area()` da `NULL`, el `WITH CHECK` del INSERT no es `TRUE` y la fila se rechaza → **solo `super_admin` puede crear un bloque sin área**, gratis. Lo mismo en la rama de director de UPDATE/DELETE. **⚠ CAVEAT EXPLÍCITO:** la rama de **subdirector** de `"page_blocks: staff update by scope"` y `"page_blocks: staff delete by scope"` es `(public.auth_role() = 'subdirector' and owner_id = auth.uid())` — **sin condición de área**. Un subdirector que fuera `owner_id` de un bloque con `area_slug = NULL` **podría editarlo y borrarlo**, saltándose el "solo super_admin" que el INSERT sí garantiza. Hoy es **inalcanzable en la práctica** por dos candados independientes: (1) los endpoints fijan `ownerId` = usuario autenticado del request, nunca viene del cliente (misma regla que Sprint 3, T3.3), y (2) el INSERT sin área ya está bloqueado para no-super_admin, así que un subdirector nunca llega a ser `owner_id` de una fila sin área. **Cuándo deja de ser inalcanzable:** si en algún momento se permite **reasignar `owner_id`** (caso realista: traspasar contenido al cambiar la directiva, o una pantalla de "cambiar responsable" en `/administrator/usuarios`), este caveat se vuelve explotable y hay que endurecer la rama de subdirector **antes** de habilitar esa función — p. ej. agregándole `and area_slug is not null`. Documentado también en el encabezado de `database/patches/003-page-blocks-drop-area-not-null.sql` y en el `comment on column` de la propia columna, para que aparezca en el dashboard de Supabase y no solo en el repo. | Sprint 5, auditoría de schema previa a T5.1 (caveat detectado por el agente durante la auditoría y documentado a pedido explícito del PO) |

| L28 | 2026-09-12 | `pnpm vitest run` de la suite completa falló 1/378 en una corrida (5 corridas siguientes: 378/378 limpio) — mismo patrón que L22 (Sprint 3), ahora con un 5º archivo de integración: `SupabaseContentRepository.pageBlocks.test.ts` se suma a `rls.integration.test.ts`, `.galleries.test.ts`, `.pointers.test.ts` y `middleware.test.ts` autenticando los mismos 3 usuarios de prueba en paralelo. No se investigó a fondo ni se tocó código (no reprodujo en 5 intentos, y L22 ya documenta la causa raíz: estado de sesión compartido en `getUser()`/storage entre clientes concurrentes). Se deja registrado para que, si vuelve a aparecer con más archivos de integración sumándose en sprints futuros, no se trate como sorpresa — la mitigación ya conocida es preferir `getSession()` sobre `getUser()` donde alcance (L22), o separar la suite de integración del resto (`vitest run --exclude` o un proyecto de Vitest aparte) si la intermitencia se vuelve más frecuente. | Sprint 5, T5.4 (verificación de la suite completa antes de commitear) |

| L29 | 2026-09-12 | **2 mejoras de performance hechas fuera de alcance de cualquier sprint** (a pedido puntual del PO en conversación, diagnosticadas primero, no adivinadas): (1) `EventsLuma.astro` cargaba el iframe de Luma en `connectedCallback()`, sin importar si el usuario llegaba a scrollear hasta esa sección — medido como el recurso más pesado de `/eventos` (~1.5s solo el documento HTML). Fix: `iframe.src` se asigna recién cuando un `IntersectionObserver` (rootMargin 200px) detecta que la sección se acerca al viewport; sin soporte de `IntersectionObserver`, carga de inmediato como antes (degradación elegante). No es el bug de L17 (ahí el candado era `hidden` + `loading="lazy"` sobre el mismo elemento; acá se observa el custom element con contenido visible real, sin ese candado). (2) `/eventos` hacía `listPhotos(gallery.id)` una vez por galería dentro de un `Promise.all` (hasta 4 queries separadas con `MAX_GALLERIES=4`, sumadas a `listGalleries()`/`listPointers()` = hasta 6 queries totales) — medido como causa dominante de que `/eventos` tardara 900ms-2.5s en `pnpm dev`. Fix: `listPhotosForGalleries(galleryIds)`, 1 sola consulta `.in('gallery_id', galleryIds)` agrupada en memoria por `galleryId`; `listPhotos()` (singular) se mantiene intacto para `/administrator/galerias/[id].astro`, donde sí corresponde pedir una sola galería. Commits `0391b20`/`2251264`, `pnpm vitest run` 384/384, verificado en navegador real (ver detalle de verificación en cada commit — nota especial: el tab de automatización de `claude-in-chrome` reporta `document.visibilityState:"hidden"` incluso con foco, lo que suprime `IntersectionObserver` por completo — limitación del arnés de automatización, no del sitio, hubo que verificar con un observer sustituido a mano en la pestaña de prueba). **Quedaron sin documentar en `MEMORY.md`/`BACKLOG.md` hasta esta sesión** — corregido acá. | Fuera de sprint, entre T5.4 y la reapertura del workstream |

| L30 | 2026-09-14 | **Node 22.6+ (y por defecto en 24, la versión de este entorno) ejecuta `.ts` directo (`node script.ts`) sin `tsx`/`ts-node`** — "type stripping": borra anotaciones de tipo, no transpila nada más. Dos consecuencias no obvias, confirmadas empíricamente antes de escribir `scripts/generate-page-snapshots.ts`: (1) los imports relativos **necesitan la extensión `.ts` explícita** (`from '../src/x.ts'`) — a diferencia de Vite, Node no adivina la extensión, así que el estilo "sin extensión" de todo `src/**` rompe si un script plano intenta importarlo directo; (2) un `import type { X } from '...'` se **elide por completo** (nunca se resuelve el módulo en runtime) porque `verbatimModuleSyntax: true` (tsconfig de Astro) ya garantiza que ese import no tiene efectos de runtime — confirmado con un import a un módulo inexistente que no rompe. Por eso `buildPageSnapshots.ts` (T5.5) es "puro" en un sentido extra: solo tiene `import type`, así que Node lo carga sin resolver nada más, y el script que sí necesita ejecutar código (`validatePageBlockData`, self-contenido, cero imports propios) lo importa con extensión explícita. **Regla general para cualquier script nuevo bajo `scripts/` que necesite reusar lógica de `src/`:** o el módulo importado es 100% self-contenido (cero imports de valor propios, como `validatePageBlockData.ts`), o se duplica la lógica mínima en el script (patrón ya establecido en `verify-cloudinary-upload.mjs`, L9) — intentar importar un módulo con su propio árbol de imports sin extensión falla con `ERR_MODULE_NOT_FOUND`. | Sprint 5, T5.5 |

## Decisiones tomadas (con su porqué)

| # | Fecha | Decisión | Porqué |
|---|---|---|---|
| D-P1 | 2026-09-10 | RLS: `super_admin` = todo · `director` = toda su área · `subdirector` = solo `owner_id = él`. | Regla de negocio del VP ("cada rol edita solo lo suyo"); aprobada por el PO. |
| D-P2 | 2026-09-10 | "Áreas" = los 6 pilares, **usando sus mismos slugs**. Se elimina una tabla `areas` separada; `pillars` es la referencia sembrada (`source='static'`). | El PO confirmó el mapeo 1:1. Evita inventar nombres. |
| D-P3 | 2026-09-10 | 2FA (TOTP) **diferido a post-piloto**; Sprint 2 queda "MFA-ready" (middleware lee AAL, no lo exige). | Equipo pequeño y no-dev; dato de bajo riesgo; el objetivo es adopción. Activarlo luego cuesta poco. |
| D-P4 | 2026-09-10 | `/eventos`: botón a Luma siempre visible + iframe opcional con timeout 8s/onerror. | Ver L3. Evita acoplar una sección de alto tráfico a que Luma cargue. |
| D-P5 | 2026-09-10 | Calendario de Luma verificado: `https://luma.com/leadutp_` (público, activo). El iframe usa el snippet de embed del panel de Luma (`cal-id`), no la URL de vanidad. Sin snippet → solo botón. | Verificación propia + docs de Luma (Calendar → Settings → Embed). |
| D-P6 | 2026-09-10 | Dominio propio `leadutp.org` → **después** del merge del panel. Fuera de alcance. | Decisión del PO. El panel funciona igual en `*.vercel.app`. |
| D-01 | 2026-09-10 | Subida a Cloudinary con **firma en endpoint propio** (no unsigned preset, no proxy). Sin SDK `cloudinary`. | Secreto server-side; no proxear MB por función serverless; firma = ~10 líneas con `crypto`. |
| D-02 | 2026-09-10 | Degradación por **snapshot `*.fallback.json`** versionado + cadena Supabase→JSON→`.data.ts`→estado vacío en build. | `pnpm build` nunca puede romper por Supabase caído; el deploy anterior sigue vivo. |
| D-03 | 2026-09-10 | Claims de rol/área en el JWT vía **Auth Hook** (`custom_access_token_hook`) para que RLS no haga subconsultas por fila. | Rendimiento de las políticas. |
| D-04 | 2026-09-10 | Nomenclatura de env vars de Supabase actualizada a las **API keys nuevas**: `PUBLIC_SUPABASE_PUBLISHABLE_KEY` (reemplaza `PUBLIC_SUPABASE_ANON_KEY`) y `SUPABASE_SECRET_KEY` (reemplaza `SUPABASE_SERVICE_ROLE_KEY`) en `TECH_STACK.md`, `docs/DISENO_TECNICO.md`, `docs/API_CONTRACTS.md`, `ARCHITECTURE.md`. Sin cambio de comportamiento (mismo privilegio, misma RLS) ni de versión mínima de `@supabase/supabase-js` (cualquier ^2 las acepta, son strings opacos a `createClient`). | Supabase migró su sistema de keys; los proyectos creados después de noviembre de 2025 (el nuestro) ya no exponen `anon`/`service_role`. Nota: `docs/GUIA_METODOLOGIA_ADMIN.md` (línea del Paso 4.1, escrita por el PO) todavía dice `SUPABASE_ANON_KEY` — señalado, no editado (fuera del alcance de "donde el agente lo mencionó"). Los roles de Postgres `anon`/`authenticated` usados en `database/schema.sql` y en las políticas RLS **no cambian de nombre** — son un concepto distinto de las API keys. |
| D-P7 | 2026-09-10 | La validación de DoD de cada sprint es **100% local** hasta que el PO conecte Vercel (`pnpm dev` para el PO; `pnpm build && pnpm preview` corrido y reportado por el agente al cerrar cada sprint). `git push origin cms-admin` sigue siendo obligatorio, pero solo como respaldo remoto. Cuando Vercel se conecte, este mismo rol se traslada a la preview URL del PR. | El proyecto aún no está conectado a Vercel; se trabaja primero 100% en local, Vercel se conecta más adelante, antes del merge final. Decisión del PO, aplicada en `docs/GUIA_METODOLOGIA_ADMIN.md`, `AGENTS.md` y `.sprints/BACKLOG.md`. |
| D-05 | 2026-09-10 | `HomeEventsCta.astro`: CTA simple (texto corto + `Button` a Luma, reutilizando `buildLumaLinks()`) reemplaza a `HomeEvents` en la Home mientras no exista un `featuredEvent` local. Deliberadamente **sin listar eventos individuales** — eso es exactamente el trabajo de Sprint 3. | El PO va a mostrar la rama `cms-admin` a la junta directiva pronto y ya hay eventos reales en Luma; prefirió no dejar la Home con un hueco silencioso durante esa ventana. Tarea extra dentro de Sprint 1 (T1.7), no alcance nuevo — el swap a `HomeEvents` real en Sprint 3 es trivial (un solo `if/else` en `index.astro`). |
| D-06 | 2026-09-10 | **Reintroducción temporal y explícitamente marcada** de 1 evento ficticio ("Study Abroad Fest 2026") en `events.data.ts`, con `FeaturedEvent`/`UpcomingEventList` restaurados en `/eventos` — `EventsLuma` queda como **complemento** ("nuestro calendario completo vive en Luma"), no como reemplazo. `registrationUrl` apunta al calendario general de Luma, no a una URL de evento inventada. **Revertido el mismo día** (2026-09-10), tras la demo: `events.data.ts` vacío de nuevo, `/eventos.astro` de vuelta a la versión de cierre de Sprint 1 (solo `EventsLuma`). Nada de esto llegó a commitearse en ningún momento. | Demo puntual a la junta directiva; el formato de tarjeta pre-Sprint-1 comunica mejor en esa reunión que el embed de Luma solo. Cambio de alcance puntual del PO, no reabrió Sprint 1. |
| D-07 | 2026-09-11 | Leer rol/área/AAL siempre con `supabase.auth.getClaims()`, nunca con `data.user.app_metadata` de `getUser()`/`signInWithPassword()`. Es la API que Supabase documenta como compatible con Auth Hooks personalizados, valida el JWT (JWKS o red) y de paso trae el `aal` sin llamada aparte. | Fix de L19. Se deja como regla explícita para que ningún código futuro (Sprint 3+) repita el error de leer `user.app_metadata` pensando que trae las claims del hook. |
| D-08 | 2026-09-11 | El merge de `cms-admin` → `main` (PR #8) y de `feature/pruebas` → `main` (PR #9, renombra `public/imagestest/` a `public/images/`) fueron **intencionales**, hechos directamente por el PO en GitHub, **antes** de lo que marca el Paso 6 de la metodología (que espera al cierre de todos los sprints). No hay ambiente de staging separado en Vercel: el PO confirmó "no tengo vercel, todo es real" — lo que está desplegado en `leadutp.vercel.app` es el único ambiente real, sin red de seguridad de un preview intermedio. `cms-admin` se sincronizó trayendo esos 2 commits desde `main` (ver sección de restauración de `CONTEXT.md`/`PENDIENTES.md` más abajo). El trabajo continúa en `cms-admin`; Sprint 3 sigue sin abrir hasta aprobación explícita del PO. | Decisión del PO: "para que puedan ir viendo la pagina" (mostrar avance real a la comunidad/junta antes del merge final formal). Aclarado explícitamente para que el estado de `Vercel` en la tabla de arriba y el D-P7 no se lean como contradictorios con el hecho de que `main` ya tiene código de `cms-admin` en producción. |
| D-09 | 2026-09-11 | `luma_event_pointers` pasa de puntero mínimo (Sprint 0) a tarjeta completa: `sort_hint` → `event_date` + `location`, `image_url`, `short_description` nuevos (`database/patches/002-event-pointers-full-card.sql`). | El PO revisó, antes de escribir código de Sprint 3, si el schema alcanzaba para el formato de tarjeta visual completo que quería mostrar en `/eventos`. No alcanzaba — el diseño original asumía que fecha/lugar los daba la API de Luma, integración descartada por costo; el director los carga a mano. Confirmado con el PO que la tabla estaba vacía (`0` filas) antes de correr la migración destructiva sobre `sort_hint`. |
| D-10 | 2026-09-11 | `image_url` de un puntero es una URL de texto plano (el director la pega), **no** una subida firmada a Cloudinary. | Evita adelantar el flujo de subida firmada que Sprint 4 (galerías) va a construir mejor y de forma reutilizable; decisión del PO al resolver la migración de schema (junto con D-09), entre 3 opciones presentadas (URL plana / subida a Cloudinary ahora / sin imagen por ahora). |
| D-11 | 2026-09-11 | El formulario de creación (galerías y punteros a Luma) usa **un solo selector "Pilar"** para todos los roles — no un selector "Pilar" + un selector "Área" separado para super_admin. Para super_admin, ese único valor fija tanto `pillarSlug` como `areaSlug` al guardar. | El PO detectó (con captura de pantalla) que ambos selectores mostraban la misma lista de opciones y terminaban con el mismo valor — duplicación real, no un malentendido. Aunque `area_slug`/`pillar_slug` **pueden** diferir por diseño (uno es para RLS, el otro es "concepto de contenido del sitio público", `DOMAIN.md` línea 23), nadie usa hoy esa flexibilidad, así que sostenerla en la UI solo agregaba confusión sin aportar nada real. Para director/subdirector, `pillarSlug` sigue siendo una etiqueta de contenido opcional e independiente de su área (ya fija por su perfil) — eso no cambió. |
| D-12 | 2026-09-11 | El límite de tamaño de subida (10 MB) es una validación de **UX, no de seguridad dura** — se hace cumplir solo en el cliente (`file.size` antes de subir), nunca del lado de Cloudinary ni de nuestro propio servidor. | Consecuencia directa de L25: Cloudinary no soporta `max_file_size` en subidas firmadas, así que no hay forma de que el servidor lo imponga en este flujo sin reintroducir un *upload preset* (descartado por diseño). Se acepta el riesgo de que alguien edite el JS del navegador y suba un archivo más pesado porque **quien sube fotos siempre es staff autenticado con permiso de editar esa galería, nunca un visitante público** — el peor caso es cuota de Cloudinary usada de más, no una brecha de seguridad. Documentado explícitamente en `docs/DISENO_TECNICO.md` §2.2 para que no se asuma en silencio ni se re-investigue como si fuera un bug pendiente. Mitigación futura si el riesgo cambiara: validar `bytes` de la respuesta de Cloudinary antes de aceptar el registro en `POST .../photos`, con `destroy()` si excede. |

| D-13 | 2026-09-11 | **`page_blocks.area_slug` pasa a nullable** (`database/patches/003-page-blocks-drop-area-not-null.sql`, migración de 1 línea, **no destructiva** — a diferencia de D-09/patch 002). `NULL` = contenido institucional (Nosotros, Proyectos), editable **solo por `super_admin`**; un bloque con `area_slug` sigue funcionando con la RLS de director tal cual, sin migrar nada. **Ninguna política de RLS cambia** — ver L27 para el mecanismo y para el **caveat de UPDATE/DELETE con subdirector** que hay que revisar si algún día se permite reasignar `owner_id`. | Auditoría de schema pedida por el PO antes de T5.1, mismo patrón que en Sprints 3 y 4. Las áreas mapean 1:1 a los 6 pilares (D-P2), pero "Nosotros" y "Proyectos" **no son un pilar**: con `NOT NULL` había que asignarles un pilar arbitrario, y como `area_slug` es lo que decide quién edita, eso significaba que el director de ese pilar quedaba como único director capaz de editar la historia o la junta directiva de toda la organización — además de violar la regla 5 de trazabilidad (el dato diría algo falso). Se descartaron: sembrar un 7º pilar `institucional` (`pillars` es world-readable y se filtraría al selector "Pilar" de D-11 y a cualquier UI que liste pilares) y dejar `NOT NULL` con un pilar de mentira (deuda silenciosa). El propio `docs/API_CONTRACTS.md` §5 ya delataba la incomodidad: le ponía `"areaSlug": "desarrollo-del-capitulo"` a `proyectos.list` sin explicar por qué. |
| D-15 | 2026-09-13 | **El VP confirmó: Proyectos es una lista centralizada**, no "cada pilar sube los suyos". Resuelve la única pieza que D-14(f) había dejado deliberadamente provisional. El modelo ya construido en T5.1-T5.4 (`proyectos.list` = `Project[]` en una sola fila de `page_blocks`, editable solo por `super_admin`) queda **confirmado tal cual, sin migración**. | Pregunta que el PO tenía pendiente con el VP desde la auditoría de schema previa a T5.1 (D-14f). Se resuelve antes de escribir T5.5, así T5.5/T5.6 cierran el sprint sobre un modelo ya definitivo, no provisional. |
| D-14 | 2026-09-11 | **Alcance de contenido de Sprint 5, confirmado por el PO (decisiones D-a…D-f de la auditoría):** (a) Nosotros y Proyectos los mantiene **solo super_admin** → D-13. (b) **Misión, visión, valores y acrónimo siguen estáticos** en `about.data.ts`; el panel solo gestiona las 3 keys ya declaradas (`nosotros.history`, `nosotros.team`, `proyectos.list`). (c) Poner en borrador un bloque ya publicado **lo saca del sitio público** en el próximo build (1 fila por `key`, 1 solo `published`) — **se acepta sin migración**, pero el panel debe avisar al despublicar algo en vivo. (d) `nosotros.history` se guarda y renderiza como **markdown**, reutilizando `renderMarkdown()` de Sprint 4 — implica cambiar **una línea** de `nosotros.astro` (`<p>{about.history}</p>` → `set:html`), sin reestructurar el layout. (e) Junta directiva se queda en **`{name, role}`**, sin fotos. (f) Proyectos se modela como **bloque único** (`proyectos.list` = `Project[]` en una sola fila), no como tabla real por proyecto. | (b) Lo que ya es real y estable no gana nada mudándose al panel; agregar después una key `nosotros.identity` no requiere migración (`data` es jsonb). (c) Un `draft_data` aparte es sobre-ingeniería para un equipo de 3 personas. (d) Hoy `about.history` es texto plano en un solo `<p>`: la directiva no podría meter ni un salto de párrafo en un texto de "cómo empezamos" — se nota al primer intento. Markdown ya está instalado, probado y sanitizado desde Sprint 4 (mismo tratamiento que el `body` de galerías = consistencia). (e) El diseño público actual de la junta no tiene fotos, y agregarlas obligaría a generalizar el endpoint de firma de Cloudinary, hoy atado a `galleryId` + `can_edit_gallery` — alcance nuevo, no Sprint 5. (f) **Decisión deliberadamente provisional:** el PO va a confirmar con el VP si cada pilar publica sus propios proyectos o si es una lista centralizada; mientras tanto se avanza con el bloque único para no bloquear el sprint por una pregunta de producto que se resuelve en paralelo. Es reversible: migrar de un `Project[]` en jsonb a una tabla `projects` con 1 fila por proyecto (patrón `luma_event_pointers`, RLS por área) no es una migración difícil. **Si la respuesta del VP es "cada pilar sube los suyos", hay que decidirlo antes de T5.2** — cambia T5.2/T5.3/T5.4 por completo. **RESUELTO — ver D-15: el VP confirmó lista centralizada, el bloque único queda definitivo.** |

## Retrospectiva de sprints

### Sprint 0 · Infraestructura base — cierre 2026-09-10

**Qué se construyó:**

| Área | Detalle |
|---|---|
| Adaptador de servidor (T0.1) | `astro.config.mjs`: `output: 'static'` explícito + `adapter: vercel()` + `sitemap({ filter: page => !page.includes('/administrator') })`. Sin ningún `prerender = false` todavía (no hay rutas `/administrator/**`). |
| Env vars tipadas (T0.2) | `src/env.d.ts` con las 6 vars de Supabase/Cloudinary (nomenclatura nueva) + 2 de Luma (opcional). |
| Esqueleto hexagonal (T0.5) | `src/domain/types.ts` (Role, AreaSlug, ContentSource, Profile, OwnedResource) + 3 puertos (`ContentRepository`, `PhotoStorage`, `AuthGateway`) + `src/infra/container.ts` (composition root) + 3 adaptadores no-op. |
| Tests nuevos | `src/domain/purity.test.ts` (Rojo→Verde: garantiza que `src/domain/**` no importa Astro/Supabase/Cloudinary/otras capas) + `src/infra/build-boundary.test.ts` (verifica 15 HTML públicos / 0 funciones serverless, se salta sin build previo). |
| Script de verificación (T0.4) | `scripts/verify-cloudinary-upload.mjs` — manual, no en CI. |

**Verificación de Cloudinary (T0.4) — resultado:**
```
✓ Subida OK: lead-utp/_test/htktah0uias9odayconu
✓ Borrado OK. Cloudinary configurado correctamente.
```
Verificado además con una **segunda llamada independiente**, de solo lectura, al Admin API (`GET /resources/image?prefix=lead-utp/_test` → `{ "resources": [] }`) — no se confió únicamente en el "Borrado OK" del propio script. Sin huérfanos.

**Decisiones/hallazgos nuevos de este sprint:** ver L7 (patrón `void nombre;` en vez de `_prefijo` para parámetros no usados), L8 (`.vercel/` debe ignorarse en `.gitignore` **y** `eslint.config.mjs`), L9 (aplicar `schema.sql` requiere acción manual del PO, sin CLI instalado).

**Estado de cierre — primera pasada (registrado, luego corregido):** el PO aprobó Sprint 0 dando por válida la parte de código (T0.1, T0.2, T0.4, T0.5, T0.6 — las 5 verificadas por el agente con tests/build en verde y validadas en local con `pnpm dev` + `pnpm build && pnpm preview`). Al preguntarle explícitamente por **T0.3** (aplicar `schema.sql` + registrar el Auth Hook), el PO confirmó en ese momento que **todavía no lo había hecho**. Sprint 0 se marcó **🟡 5/6** y se commiteó así (`412f3e9`).

**T0.3 — cierre real (2026-09-10, mismo día):** el PO corrió `database/schema.sql` completo en el SQL Editor de Supabase, sin errores ni warnings, y verificó dos cosas **a mano en el dashboard**:
1. **Table Editor** — las 6 tablas esperadas existen: `profiles`, `pillars`, `luma_event_pointers`, `event_galleries`, `gallery_photos`, `page_blocks`.
2. **Authentication → Policies** — las políticas RLS se crearon correctamente.

Esta verificación **es manual, del PO, en el dashboard de Supabase — no por código ni por test automatizado del agente** (a diferencia de T0.1/T0.2/T0.4/T0.5/T0.6, que sí tienen test o build en verde como evidencia). Queda documentado así explícitamente porque este tipo de tarea (aplicar DDL en un proyecto Supabase real, sin CLI instalado) se valida de forma distinta al resto del sprint: es responsabilidad exclusiva del PO, el agente no tiene medio para ejecutarla ni para inspeccionar el dashboard directamente.

El agente sí pudo dar una **corroboración independiente de solo lectura** después de la confirmación del PO: `GET /rest/v1/pillars?select=slug,name` con `PUBLIC_SUPABASE_PUBLISHABLE_KEY` → HTTP 200, las 6 filas sembradas (`desarrollo-profesional`, `liderazgo`, `excelencia-femenina`, `desarrollo-del-capitulo`, `excelencia-academica`, `lead-academia`). Esto confirma que el schema y al menos la política "pillars: world readable" quedaron activos — no reemplaza la verificación del PO sobre las políticas de las demás tablas (esas no son legibles por `anon`, así que no hay forma de corroborarlas sin credenciales de más privilegio; eso es justamente lo que valida Sprint 2, T2.6).

**Sprint 0: ✅ DONE, 6/6.** Sprint 1 sigue sin arrancar — requiere confirmación explícita del PO.

### Sprint 1 · Eventos + embed Luma (piloto) — cierre 2026-09-10

**Qué se construyó:**

| Área | Detalle |
|---|---|
| Verificación de Luma (previa a implementar) | `curl -I` a `luma.com/leadutp_` (bloquea framing) y a `luma.com/embed/calendar/leadutp_/events` (no bloquea, sin CSP frame-ancestors, sin JS anti-framing, contenido confirmado). Ver L10. |
| Dominio (T1.1) | `src/domain/lumaLinks.ts` + `buildLumaLinks()`, Rojo→Verde con `lumaLinks.test.ts` (3 tests). |
| Componente (T1.2/T1.3) | `src/components/events/EventsLuma.astro`: botón siempre visible (Button primary/lg) + `<events-luma-embed>` (custom element) con fallback timeout 8s + `load`/`error`. `Button.astro` extendido con `target`/`rel` opcionales (L11). |
| `/eventos` (T1.4/T1.5) | `events.data.ts` vaciado (22 eventos ficticios fuera, comentario explicando por qué). Sección "Próximos" = `EventsLuma`; "Pasados" = estado vacío curado con link real a `/vida-lead`. |
| Extra aprobado por el PO (T1.7) | `src/components/home/HomeEventsCta.astro`: CTA simple en la Home mientras no haya `featuredEvent` local (ver D-05 y L12). Reutiliza `buildLumaLinks()`, sin listar eventos. |

**Verificación:** `astro check` 0 errores (86 archivos) · `pnpm test` 33/33 · `pnpm lint` limpio · `pnpm build` 15 páginas / 0 funciones (`build-boundary.test.ts` contra build fresco) · HTML del build inspeccionado a mano: botón apunta a `https://luma.com/leadutp_`, 0 iframes sin `PUBLIC_LUMA_EMBED_URL` configurada.

**Recomendación pendiente de aplicar por el PO:** agregar `PUBLIC_LUMA_EMBED_URL=https://luma.com/embed/calendar/leadutp_/events` al `.env` para activar el iframe (opcional — sin ella, el sitio funciona igual solo con el botón).

**Verificación visual:** no se pudo tomar una captura real (el PO declinó instalar la extensión de Chrome en esta sesión y no hay `chromium-cli`/Playwright configurado). Se verificó por HTML servido por el propio `pnpm dev` del PO — clases idénticas a las de componentes `Button`/`Container` ya probados visualmente en producción. **El PO revisó `pnpm dev` directamente antes de aprobar el cierre.**

**Efecto en cascada encontrado y resuelto:** vaciar `events.data.ts` apagaba silenciosamente la sección de eventos de la Home (código preexistente, `{featuredEvent && ...}`). Se le avisó al PO antes de decidir — eligió agregar el CTA (D-05, T1.7) en vez de dejarlo así, por una demo próxima a la junta directiva.

**Sprint 1: ✅ DONE, 6/6 + 1 extra (T1.7).** Reportado, esperando aprobación explícita del PO antes de cualquier commit. Sprint 2 no arranca sin esa confirmación.

### Sprint 2 · Autenticación y roles — cierre 2026-09-11

**Qué se construyó:**

| Área | Detalle |
|---|---|
| Middleware (T2.1/T2.3) | `src/middleware.ts`: guard sobre `/administrator/**` excepto `/administrator/login`; sin sesión → `context.rewrite()` (200, no redirect); con sesión → `Astro.locals.profile`. Rojo→Verde con `middleware.test.ts` (mockea el `AuthGateway`, no toca Supabase). |
| Adaptador de Auth (T2.2) | `src/infra/supabase/{cookieHeader, mapUserToProfile, createCookieAdapter, SupabaseAuthGateway}.ts` — piezas puras con TDD completo; `container.ts` expone `getAuthGatewayForRequest(context)`. `NoopAuthGateway` eliminado (superado). |
| Panel (T2.4/T2.5) | `/administrator/login.astro` (form usuario+contraseña, error genérico) · `/administrator/index.astro` (dashboard shell, saludo por rol) · `/administrator/logout.astro` (solo POST) · `AdminShell.astro`/`Field.astro`/`AdminLayout.astro` (DESIGN.md §5-6, sin Navbar/Footer públicos). |
| RLS real (T2.6) | `src/infra/supabase/rls.integration.test.ts` — 7/7 verde contra Supabase real, las 6 fronteras de la matriz de `DOMAIN.md` + creación por director. Limpia sus filas de prueba solo. |
| Ruta oculta (T2.7) | `robots.txt` con `Disallow: /administrator` (faltaba) + `src/infra/admin-route-hidden.test.ts` (automatiza sitemap/robots/sin-enlaces). |
| Build (T2.8) | `astro.config.mjs` ya tenía el adaptador (Sprint 0); primera vez con una ruta real `prerender=false` → 1 función (`_render.func`, agrupa las 3 rutas de `/administrator`). `build-boundary.test.ts` actualizado a `EXPECTED_FUNCTION_COUNT = 1`. |

**2 bugs reales encontrados y corregidos en el camino (no en el código feliz, en la integración real):**

1. **L18** — `custom_access_token_hook` (Sprint 0) sin `SECURITY DEFINER`: al activar el hook, el login se rompía con "permission denied for table profiles". Fix: `database/patches/001-fix-hook-security-definer.sql`.
2. **L19** — `SupabaseAuthGateway` leía `data.user.app_metadata` (de `getUser()`/`signInWithPassword()`) en vez de `getClaims()`: el perfil nunca se armaba, y el login manual fallaba con "usuario o contraseña incorrectos" aunque las credenciales fueran correctas (el test de RLS no lo detectó porque no pasa por esta clase). Fix + regla general en D-07.

Ambos bugs se encontraron **porque el PO insistió en probar el flujo real** (RLS con Supabase de verdad, login manual en el navegador) en vez de conformarse con los tests unitarios/mockeados — ninguno de los dos se hubiera visto solo con `pnpm test`.

**Verificación final:** `tsc --noEmit` 0 errores · `pnpm test` 100/100 (incluye las 7 de RLS contra Supabase real) · `pnpm lint` limpio · `pnpm build` 15 páginas públicas + 1 función · **login y logout confirmados por el PO en el navegador con los 3 usuarios reales**.

**Sprint 2: ✅ DONE, 8/8.** Commits: `2938fe7` (feat, con la versión con bug de `SupabaseAuthGateway` para reflejar la historia real) → `22a6679` (fix) → cierre de docs (este commit). Sprint 3 no arranca sin confirmación explícita del PO.

### Sprint 3 · CRUD de eventos propios — cierre 2026-09-11

**Antes de escribir código:** revisión del schema pedida explícitamente por el PO — ¿`luma_event_pointers` alcanza para la tarjeta visual completa (título, fecha, ubicación, imagen, descripción corta, URL de registro) que se quiere mostrar en `/eventos`? No alcanzaba: el diseño de Sprint 0 asumía datos autoritativos solo en Luma (API descartada por costo). Ver D-09/D-10 y `database/patches/002-event-pointers-full-card.sql`.

**Qué se construyó:**

| Área | Detalle |
|---|---|
| Dominio (T3.1) | `canEdit(actor, resource)` — matriz completa (super_admin / director por área / subdirector por `ownerId`), espejo exacto de la RLS `"staff update by scope"` de `schema.sql`. Rojo→Verde con `canEdit.test.ts` (9 tests); un test inicial estaba mal escrito (asumía que subdirector también necesitaba coincidencia de área) — corregido contra el contrato real de `DOMAIN.md`/RLS, no al revés. |
| Repositorio (T3.2) | `SupabaseContentRepository`: `listPointers/createPointer/updatePointer/deletePointer`, puerto `ContentRepository` extendido con `EventPointer`. 8 tests de contrato contra Supabase real con las 3 cuentas de prueba (RLS + mapeo camelCase↔snake_case). Bug real encontrado y corregido: L22. |
| Endpoints (T3.3) | `/administrator/api/event-pointers` (GET/POST) y `/:id` (PATCH/DELETE) — `validateEventPointerInput`/`validateEventPointerPatch` (dominio puro, reglas compartidas via `validateOptionalFields`) antes de tocar la DB; `areaSlug`/`ownerId` nunca vienen del cliente; autorización 100% delegada a la RLS. Bug real encontrado y corregido: L21 (archivos de test rompían el build). |
| UI del panel (T3.4) | `/administrator/eventos`: lista con chip de área, badge Borrador/Publicado, acciones `Editar`/`Borrar` condicionadas a `canEdit()` (la RLS deja leer todo al staff — `"staff reads all"` — así que la lista no se filtra, solo las acciones); `<dialog>` nativo con el formulario completo; custom element vanilla (sin framework, mismo patrón que `EventsLuma`) que hace `fetch()` a los endpoints. `Field.astro` extendido a `input`/`textarea`/`select`. `AdminShell` activa el link de Eventos. |
| Web pública (T3.5) | `selectFeaturedPointers()` (dominio puro, `published && featured`) + `EventPointerCard.astro` (mismo lenguaje visual que `EventCard.astro`) en `/eventos`, junto al embed de Luma. `getContentRepository()` deja de ser no-op: cliente Supabase anon en build-time (degrada a `NoopContentRepository` si faltan las env vars; `try/catch` en la página si Supabase falla en tiempo de build). |
| Build (T3.6) | Ver L20 (`pnpm preview` no soportado por `@astrojs/vercel`, no es bug). |

**3 bugs reales encontrados y corregidos en el camino** (ninguno en el "camino feliz" de un test aislado — todos aparecieron corriendo la suite completa, el build completo, o probando a mano con `curl`/`pnpm dev`):

1. **L21** — `.test.ts` dentro de `src/pages/` rompía `pnpm build` (Astro los trata como rutas). Fix: prefijo `_`.
2. **L22** — carrera de sesiones concurrentes de Supabase Auth entre archivos de test que autentican el mismo usuario; `getUser()` (red) fallaba, `getSession()` (local) no. Fix en `SupabaseContentRepository.createPointer`.
3. **L23** — CSRF nativo de Astro (`Origin` header) devolvía 403 en pruebas manuales con `curl` sin ese header — no es bug del endpoint, es el comportamiento esperado (un navegador sí lo manda).

**Verificación final:** `pnpm vitest run` 167/167 · `pnpm lint` limpio · `pnpm build` limpio (15 HTML públicos, 1 función admin) · **CRUD real de punta a punta** con las 3 cuentas de prueba vía `pnpm dev` (login → crear puntero → aparece en el panel y en `/eventos` público con fecha en español/ubicación/link a Luma, sin imagen rota al no cargar una → editar → borrar, `204`, sin dejar filas huérfanas). `pnpm preview` no corrió — ver L20, no es parte del DoD.

**Sprint 3: ✅ DONE, 6/6.** Commits: `c3a822b` (feat) → `32c10fa` (docs: migración de schema) → cierre de docs (este commit). Sprint 4 no arranca sin confirmación explícita del PO.

### Sprint 4 · Galería "así se vivió el evento" — cierre 2026-09-11

**Antes de escribir código:** revisión pedida por el PO, igual que en Sprint 3 — ¿el diseño de subida a Cloudinary (`DISENO_TECNICO.md` §2, opción B: firma en endpoint propio) seguía vigente, y el schema de `event_galleries`/`gallery_photos` alcanzaba? A diferencia de `luma_event_pointers` en Sprint 3, **este sí alcanzaba tal cual, sin migración** — coincidía campo por campo con lo aprobado, y la RLS ya estaba aplicada desde T0.3.

**Qué se construyó:**

| Área | Detalle |
|---|---|
| Firma (T4.1-T4.3) | `buildSignature()` (SHA-1, `node:crypto`, sin SDK) · endpoint `POST /administrator/api/uploads/sign` (`canEditGallery()` vía RPC `can_edit_gallery`, reutiliza la función SQL de Sprint 0) · `CloudinaryPhotoStorage` (`sign`/`deliveryUrl`/`destroy`). Límites de seguridad agregados a pedido del PO: `allowedFormats`/`maxFileSize` en la firma — con el bug real de L25 encontrado y corregido después. |
| CRUD de galería/fotos (T4.4) | Repositorio extendido (`listGalleries` con `photoCount` vía join, `createGallery` con `GallerySlugConflictError`, `updateGallery`, `deleteGallery`, `listPhotos`, `createPhoto` con `position` autocalculada, `updatePhoto`, `deletePhoto`) + 6 endpoints REST, todos con borrado best-effort de Cloudinary. |
| UI del panel (T4.5) | `/administrator/galerias` (lista + creación) y `galerias/[id].astro` (metadata + fotos: subida con barra de progreso real vía XHR, reordenar con swap de `position`, editar `alt`, borrar). `Field.astro` extendido con `value` (prefill de edición). |
| Web pública (T4.6) | `GalleryEventCard.astro` (mismo lenguaje visual que `LifeEventCard.astro` de `/vida-lead`) + `renderMarkdown()` (`marked`+`sanitize-html`) para el texto de la galería — degradación con placeholder local si una imagen de Cloudinary no carga. |
| Build (T4.7) | Ver más abajo. |

**5 bugs reales encontrados y corregidos, todos durante la validación con la UI real (no con `pnpm test`) — el PO los reportó probando el panel de verdad, clic a clic, no solo revisando código:**

1. **Bug 1 / L24** — `Button.astro` no reenviaba props `data-*` al DOM: ningún botón con gancho de JS respondía al clic, en **ninguna** pantalla del panel (afectaba también `eventos.astro` de Sprint 3, sin que nadie lo hubiera notado). Fix: `...rest` en `Button.astro`.
2. **Bug 2** — el sidebar (`AdminShell.astro`) nunca tuvo el link a "Galerías" activado — quedó como placeholder "Pronto" por descuido.
3. **Hallazgo del PO (punto 3 de su propio bug report)** — el form de creación no pedía `areaSlug`, así que un `super_admin` (sin área propia) no podía crear ni galería ni puntero desde el panel — 422 garantizado. Agregado un campo "Área" condicional, luego **fusionado** con "Pilar" en un solo selector (D-11) al notar la duplicación.
4. **Markdown sin parsear** — `gallery.body` se guardaba y mostraba como texto plano pese a llamarse "Texto (markdown)" desde Sprint 0; nunca hubo un parser instalado. Agregado `renderMarkdown()` con sanitización (`marked`+`sanitize-html`), verificado con un intento real de `<script>`/`<img onerror>` que quedó completamente eliminado del HTML público.
5. **L25** — `max_file_size` no es válido para subidas firmadas de Cloudinary (bug encontrado subiendo 2 fotos `.webp` reales que el PO tenía a mano) — causaba "Invalid Signature" para **toda** subida, sin importar formato ni tamaño. Confirmado con la documentación oficial de Cloudinary (vía WebFetch) y con pruebas directas contra la cuenta real. Fix + trade-off aceptado documentado en D-12.

**Verificación final:** `pnpm vitest run` 285/285 · `pnpm lint` limpio · `pnpm build` limpio (15 HTML públicos, 1 función admin) · **flujo real de punta a punta** con las 3 cuentas de prueba vía `pnpm dev`: crear galería → subir fotos reales a Cloudinary (incluidas las 2 que dispararon L25) → verlas en el panel y en `/eventos` público con markdown renderizado → editar → reordenar → borrar con limpieza best-effort en Cloudinary — sin datos huérfanos en ningún momento. `pnpm preview` no corrió — ver L20.

**Sprint 4: ✅ DONE, 7/7.** Commits: `0542b44` (fix: `max_file_size`, con las piezas de infraestructura Cloudinary/ContentRepository que arrastró por no tener commits previos ese sprint — ver L26) → `ab65848` (feat: CRUD de galerías/fotos + UI del panel + fixes de Button/sidebar/pilar-área) → `7f8c367` (feat: galerías en `/eventos` público + markdown) → cierre de docs (este commit). Sprint 5 no arranca sin confirmación explícita del PO.

### Sprint 5 · Extensión a Nosotros y Proyectos — cierre 2026-09-14

> El PO dio la confirmación explícita para arrancar Sprint 5 en una sesión posterior al cierre de Sprint 4; T5.1-T5.4 se hicieron ahí (2026-09-11/12) pero **esta retrospectiva quedó sin escribirse hasta reabrir el workstream** (2026-09-13) — ver la nota de proceso en `.sprints/BACKLOG.md`. Reconstruida a partir de los mensajes de commit, que ya traían el detalle Rojo→Verde y de verificación.

**Antes de escribir código:** auditoría de schema pedida por el PO, mismo patrón que Sprints 3 y 4 — `page_blocks.area_slug` era `NOT NULL`, pero "Nosotros" y "Proyectos" no son un pilar (D-P2 mapea áreas 1:1 a los 6 pilares). Se optó por hacer la columna nullable (D-13, `database/patches/003-page-blocks-drop-area-not-null.sql`) en vez de sembrar un 7º pilar falso o forzar un pilar arbitrario — con el caveat de RLS documentado en L27.

**Qué se construyó:**

| Área | Detalle |
|---|---|
| Validador (T5.1) | `validatePageBlockData.ts` — la forma de `data` jsonb por `key` (`nosotros.history` → `{ body }` markdown, `nosotros.team` → `TeamMember[]`, `proyectos.list` → `Project[]`), dominio puro, sin importar `src/data/**/*.types.ts`. `Project.link` restringido a `https://` o ruta interna `/...` (nunca `javascript:`/`data:`/`http://`/protocol-relative). 34 tests, Rojo→Verde. |
| Repo + endpoints (T5.2) | `getPageBlock`/`savePageBlock` en `SupabaseContentRepository` — upsert en **2 pasos** (leer → insert o update), nunca `.upsert()`, para que `owner_id` quede fijo desde el primer PUT (precondición que hace inalcanzable el caveat de L27). `area_slug` siempre `null` para estas keys, nunca viene del cliente. `GET/PUT /administrator/api/pages/:key`: 404 (key fuera de la lista cerrada, antes de tocar la DB), 400 (JSON inválido), 422 (con la ruta del campo), 403 (RLS). 13 tests de contrato contra Supabase real + 12 del endpoint mockeado. |
| UI del panel (T5.3) | `/administrator/paginas`: 3 secciones (Historia / Junta / Proyectos), cada una con su propio estado Publicado/Borrador/Sin guardar. Solo `super_admin` ve los botones de guardar (D-13); el resto del staff entra en modo lectura con aviso explícito. `pageBlockForms.ts` (dominio puro): descarta filas completamente vacías del formulario (agregar-y-arrepentirse), nunca una a medio llenar. D-14(c) implementado: confirmación antes de despublicar algo en vivo. Prefill desde `.data.ts` cuando aún no hay bloque en Supabase. `AdminShell` activa el link de Páginas (evitando repetir el bug 2 de Sprint 4). |
| Loaders públicos (T5.4) | `resolvePageContent.ts` (política pura, la cadena DB→snapshot `*.fallback.json`→`.data.ts`→vacío) + `loadPageContent.ts` (único glue de I/O; el snapshot todavía no existe — `import.meta.glob` devuelve `{}` en vez de romper, eso es T5.5). Un `data` guardado con forma inválida (ej. escrito antes de un cambio de validador) se ignora y la cadena sigue bajando, igual que si Supabase no respondiera. `nosotros.astro`/`proyectos.astro` no cambian de estructura — único cambio de layout real: la historia se renderiza con `renderMarkdown()` (D-14d) en vez de texto plano. |

**Verificado contra Supabase/dev server reales en cada tarea** (no solo con tests): PUT de historia/junta/proyectos vía `super_admin` → aparecen en `/nosotros` y `/proyectos`; despublicar vuelve al estado vacío curado; `director` recibe 403; key desconocida 404; 422 con ruta de campo incluido el rechazo de `link` con `javascript:`; vista de `director` confirmada en modo lectura sin botones de guardado; los 3 bloques de prueba se limpiaron a `published:false` al terminar (no le correspondía al agente dejar contenido institucional fabricado en vivo).

**Sin bugs nuevos encontrados en el camino** (a diferencia de Sprints 3-4) — sí una intermitencia ya conocida documentada en L28 (1/378 en una corrida de test, no reprodujo, mismo patrón que L22).

**Estado real a 2026-09-13:** 4/6. Faltan **T5.5** (script `prebuild` que escriba `about.fallback.json`/`projects.fallback.json` — hoy no existen, ni hay script `prebuild` en `package.json`) y **T5.6** (build + validación final del sprint). D-14(f) quedaba deliberadamente provisional (bloque único de Proyectos a la espera de que el VP confirmara el modelo) — **resuelto: ver D-15, el VP confirmó lista centralizada**, así que T5.5/T5.6 cierran sobre un modelo ya definitivo, sin necesidad de migrar a una tabla `projects`.

**Trabajo adicional fuera de alcance de sprint, en el camino:** 2 mejoras de performance en `/eventos` (iframe de Luma diferido con `IntersectionObserver`, `listPhotosForGalleries` en 1 query) — ver L29.

**T5.5 — script `prebuild` (2026-09-14):** `buildPageSnapshots.ts` (política pura, 14 tests Rojo→Verde) decide qué le corresponde a cada snapshot dado lo que ya había en disco + lo que se pudo leer de Supabase ahora — Supabase caído preserva el snapshot existente tal cual, una key despublicada lo limpia, contenido publicado lo reemplaza. `scripts/generate-page-snapshots.ts` es el I/O: corre con Node nativo sin `tsx`/`ts-node` (L30), revalida cada fila con el mismo `validatePageBlockData` del endpoint (T5.1) antes de confiar en su forma, y queda wireado como `"prebuild"` en `package.json` (hook de pnpm/npm antes de `build`, confirmado que pnpm 11.24 lo corre sin config adicional).

**T5.6 — build + validación final (2026-09-14):** verificado de punta a punta contra Supabase y el dev server reales, no solo con tests — mismo rigor que Sprints 2-4:
- Login real como `super_admin` (cookies de sesión vía `curl`), PUT de una historia + junta + proyecto de prueba → `/nosotros` y `/proyectos` los reflejan de inmediato en `pnpm dev` (lectura en vivo, T5.4).
- Un `director` intentando `PUT /administrator/api/pages/proyectos.list` recibe `403` (D-13: solo `super_admin` edita contenido institucional).
- `pnpm build` completo con ese contenido real publicado: el log del propio `prebuild` confirma `about.fallback.json`/`projects.fallback.json` escritos, y el HTML generado de `/nosotros`/`/proyectos` contiene el texto publicado — la cadena DB→build funciona de punta a punta, no solo en dev.
- **La prueba que de verdad importa de T5.5/T5.6:** con `PUBLIC_SUPABASE_URL` roto (simulando una caída real) para todo el comando `pnpm build` — que corre tanto `prebuild` como `astro build` bajo el mismo entorno — el log muestra `prebuild` atrapando el `fetch failed` sin tocar los snapshots, y el HTML generado de `/nosotros`/`/proyectos` sigue mostrando el contenido del snapshot (no vacío, no el `.data.ts` estático) — la cadena completa de degradación (regla inquebrantable 4 + D-02) funciona con una caída real, no solo en el unit test de `buildPageSnapshots.test.ts`.
- Los 3 bloques de prueba se revirtieron a `published: false` y los 2 snapshots fabricados se borraron corriendo `prebuild` contra el estado real (nada publicado hoy) — no le correspondía al agente dejar contenido institucional fabricado ni snapshots de prueba versionados en el repo (mismo criterio que T5.4). Confirmado con una lectura anon después: `[]` filas visibles, igual que antes de empezar.
- Un `Gateway Timeout` transitorio de Supabase durante la reversión (1 de 3 PUTs) no se investigó — reintentado y resuelto al segundo intento, consistente con L28 (intermitencia ya conocida en llamadas concurrentes/sucesivas contra el proyecto de prueba).

**Verificación final:** `pnpm vitest run` 398/398 (384 + 14 de `buildPageSnapshots.test.ts`) · `pnpm lint` limpio · `tsc --noEmit` 0 errores · `pnpm build` limpio en su estado final (15 HTML públicos, 1 función admin, `prebuild` sin salida porque hoy no hay contenido publicado en ninguna de las 3 keys — estado honesto, no un fallback fabricado). `astro check` reporta 1 error preexistente en `Field.astro:86:19` (implicit `any` en un `.map`) — confirmado con `git stash` que ya existía antes de esta sesión, no introducido por Sprint 5; queda fuera de alcance de este cierre.

**Sprint 5: ✅ DONE, 6/6.** Commits: `5d87dbe` (T5.1) → `1b6abe5` (T5.2) → `f8bd5e7` (T5.3) → `27e3dfa` (T5.4) → `dcc00a1`/dos perf commits fuera de sprint → `af67c73` (docs: sincroniza BACKLOG/MEMORY con T5.1-T5.4) → `bde5577` (T5.5) → cierre de docs (este commit). **Reportado, esperando aprobación explícita del PO antes de abrir Sprint 6** (mismo criterio que el cierre de cada sprint anterior).

## Errores / correcciones

| # | Fecha | Qué pasó | Corrección |
|---|---|---|---|
| E1 | 2026-09-10 | En el Paso 1 se entregó `REQUISITOS_ADMIN.md` sin el resumen de los audios del VP (no se había pegado). | El PO lo pegó después; se incorporó en §3/§7 y se marcaron los puntos A/B como resueltos. |
| E2 | 2026-09-10 | Diseño inicial del Paso 2 modelaba una tabla `areas` propia. | P2 la reemplazó por `pillars` (slugs de los 6 pilares). Reflejado en `schema.sql` y `DOMAIN.md`. |
